<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dbfitnesworld">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".dietlog"></activity>
        <activity android:name=".Settings" />
        <activity
            android:name=".stepCounter"
            android:label="@string/title_activity_step_counter"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Donatepage"
            android:label="@string/title_activity_donatepage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Wall" />
        <activity android:name=".CustomDialougeAddExercise" />
        <activity android:name=".Home" />
        <activity android:name=".RegisterNextActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>